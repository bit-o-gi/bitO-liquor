# Implementation Plan

## 상태 규칙
- `[ ]` 미착수
- `[-]` 진행 중
- `[x]` 완료

## 방향
- 이번 이슈는 헤더 브랜드 영역 보강과 상단 복귀 인터랙션 추가에 한정한다.
- 기존 검색/목록 로딩 로직은 건드리지 않고, 헤더 표시와 클릭 동작만 최소 범위로 수정한다.

## [x] Phase 1. 이슈 생성 및 범위 확정
### 목표
- 요구사항을 별도 이슈 문서로 고정한다.

### 작업
- [x] `005.header-logo-scroll-top` 이슈를 생성한다.
- [x] 로고 이미지 추가와 상단 스크롤 동작만 이번 범위로 한정한다.

### 완료 기준
- 작업 범위와 비범위가 문서에 정리되어 있어야 한다.

## [x] Phase 2. 헤더 로고 및 인터랙션 구현
### 목표
- 브랜드 이미지를 추가하고 로고 클릭 시 최상단으로 이동시킨다.

### 작업
- [x] 프로젝트 정적 로고 자산을 추가한다.
- [x] `CatalogPageClient.tsx` 헤더 브랜드 영역에 이미지와 텍스트를 함께 노출한다.
- [x] 브랜드 클릭 시 스무스 스크롤만 수행하도록 동작을 정리한다.

### 완료 기준
- 로고 이미지가 보이고, 클릭 시 최상단 복귀가 동작해야 한다.

## [x] Phase 3. 검증 및 문서 마감
### 목표
- 변경이 빌드/린트 기준으로 문제 없는지 확인하고 기록한다.

### 작업
- [x] 관련 변경사항을 `progress.md`에 기록한다.
- [x] `frontend`에서 `npm run lint`를 실행한다.
- [x] 검증 결과를 문서에 반영하고 마감한다.

### 완료 기준
- 검증 결과와 잔여 리스크가 문서에 남아 있어야 한다.
